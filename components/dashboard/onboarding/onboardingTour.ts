/**
 * Story 9.1 — Mini-tours driver.js contextuels par tâche d'onboarding.
 *
 * Phase 3 (placeholder) : exporte un `runMerchantTour(taskId)` qui ne fait rien
 * encore — le contenu des steps + l'init driver.js sont câblés en Phase 4.
 *
 * Cette signature est stable : OnboardingCoach + TaskItem peuvent l'appeler
 * sans changement quand Phase 4 atterrit.
 */

import type { MerchantOnboardingTaskId } from '@/lib/onboarding/getMerchantTaskStatus'

// Intentionnellement vide en Phase 3 — câblage driver.js en Phase 4.
export function runMerchantTour(_taskId: MerchantOnboardingTaskId): void {
  // no-op
}
